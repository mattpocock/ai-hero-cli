---
"ai-hero-cli": patch
---

Support exercises with only README.md (no main.ts). Linting now skips main.ts errors if readme.md exists in a subfolder, and exercise execution displays the readme path instead of failing.
