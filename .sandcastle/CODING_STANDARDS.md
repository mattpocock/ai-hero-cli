Use Effect wherever possible. Use Effect's FileSystem-style built-ins wherever possible.

---

For public-facing changes, use a changeset. They are done in `@changesets/cli`-style. Don't use major version bumps, only patch and minor.
